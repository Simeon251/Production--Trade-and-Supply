[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "energy-trade-supply-analysis"
version = "1.0.0"
description = "Professionalized predictive analytics workflow for UN energy production, trade, and supply data."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
  "joblib>=1.3",
  "matplotlib>=3.8",
  "numpy>=1.26",
  "pandas>=2.1",
  "scikit-learn>=1.4",
]

[project.optional-dependencies]
dev = [
  "jupyter>=1.1",
]

[tool.setuptools]
py-modules = []

[tool.setuptools.packages.find]
include = ["src*"]
