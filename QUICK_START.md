# Quick Start

## Install

```bash
python -m pip install -r requirements.txt
```

## Run

```bash
python run_analysis.py
```

## Test

```bash
python -m unittest discover -s tests
```

## Output

The command writes `results/analysis_summary.json` and prints the strongest model for each hypothesis.
